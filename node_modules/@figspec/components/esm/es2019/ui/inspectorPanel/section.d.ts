import { el } from "../../dom.js";
interface SectionProps {
    title: string;
    body: NonNullable<Parameters<typeof el>[2]>;
    icon?: "close" | "copy";
    onIconClick?(): void;
}
export declare function section({ title, body, icon: iconType, onIconClick, }: SectionProps): HTMLElement;
export {};
